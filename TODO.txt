crashreport TODO

- Fix max execution time error
- Ability to filter reports by product, IP, etc
- Ability to link to a single report (with stack expanded by default)
- Auto-load 'pagination'
- Fix scroll bar (shows up when it shouldn't)